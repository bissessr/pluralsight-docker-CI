version: 2
jobs:
  build:
    docker: # use the docker executor type; machine and macos executors are also supported
      - image: circleci/node:4.8.2 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
  #    - run: echo "hello world" # run the `echo` command


deployment:
  dockerhub:
  branch: master
  commands:
    - $DOCKER_HUB_TRIGGER
tests:
  override:
  - mocha

  # curl -H "Content-Type:application/json" --data '{"build":true}' -X POST https://registry.hub.docker.com/u/bissessd/pluralsight-docker-ci/trigger/57b397b9-a44c-4dda-befe-0810ba258d5f/


