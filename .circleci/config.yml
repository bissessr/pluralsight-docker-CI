version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7.10
    working_directory: ~/repo

    steps:
      - checkout
      - run: npm install
      - run: npm test

    deployment:
      dockerhub:
      branch: master
      commands:
        - $DOCKER_HUB_TRIGGER
  